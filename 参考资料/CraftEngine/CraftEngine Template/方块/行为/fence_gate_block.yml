# Template: fence_gate_block
# Wiki: configuration/block/behaviors/fence_gate_block.md

# Required block properties (auto-attached by CraftEngine):
#   open (boolean), powered (boolean), in_wall (boolean), facing (horizontal_direction)

blocks:
  custom:your_fence_gate:                             # << CHANGE THIS: 方块ID
    behavior:
      type: fence_gate_block
      can_open_with_hand: true                        # << CHANGE THIS: 允许右键点击切换 (默认 true)
      can_open_by_wind_charge: true                   # << CHANGE THIS: 允许风弹切换 (默认 true)
      sounds:
        open: block.fence_gate.open                   # << CHANGE THIS: 打开音效ID (可选)
        close: block.fence_gate.close                 # << CHANGE THIS: 关闭音效ID (可选)
