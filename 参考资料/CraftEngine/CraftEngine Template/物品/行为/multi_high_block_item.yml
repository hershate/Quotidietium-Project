# 🔢 多层方块物品 (Multi-High Block Item)
# 用于放置多格高的方块（如门），解决仅使用标准方块物品时的放置错误。

items:
  example:my_door:
    behavior:
      # 行为类型：多层方块物品
      type: multi_high_block_item

      # 【必需】放置时使用的方块 ID
      # 指定当物品被使用时放置的方块。
      block: example:my_door
